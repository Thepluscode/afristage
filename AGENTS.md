# AfriStage — agent instructions

This repository's instructions live in **[CLAUDE.md](CLAUDE.md)**, and the
mandatory product-building standard it enforces lives in
**[docs/PRODUCT_BUILDING_STANDARD.md](docs/PRODUCT_BUILDING_STANDARD.md)**.

Read both before planning or writing code. This file exists so Codex, Cursor,
Gemini CLI and other agents that look for `AGENTS.md` find the same instructions;
it is deliberately a pointer rather than a copy, so the two cannot drift apart.
